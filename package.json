{
  "name": "angular-config-vars",
  "version": "1.0.0",
  "description": "Inject environment variables in Angular",
  "main": "index.js",
  "dependencies": {
    "add-stream": "^1.0.0",
    "express": "^4.13.3",
    "gulp": "^3.9.0",
    "gulp-ng-config": "^1.2.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
